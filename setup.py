from setuptools import setup, find_packages

setup(
    name="my_score_board",
    version="1.0.0",
    author="Waleed",
    description="Live Score board library implementation",
    packages=find_packages(),
)